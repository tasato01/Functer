export const AudioManager = {
    playBGM: (_type: 'title' | 'level_select' | 'game') => {
        // Placeholder: console.log(`[Audio] Play BGM: ${type}`);
        // Implementation will depend on user assets
    },
    playSE: (_type: 'click' | 'start' | 'goal' | 'gameover' | 'waypoint' | 'alert') => {
        // Placeholder: console.log(`[Audio] Play SE: ${type}`);
        // Implementation will depend on user assets
    },
    stopBGM: () => {
        // Stop current BGM
    }
};
